# Animation — sell / collect to credits, Everdell-style

Prepend the shared spec from [00-everdell-motion.md](00-everdell-motion.md), then this prompt.
This re-flavours the existing token-spend animation (see `01-token-pile-spend.md` /
`proto-price-wall.html`) toward Everdell weight + arc.

```
[MOTION LANGUAGE] + Re-flavour the SELL collection in Everdell's tactile style: the selected hand
cards lift together (anticipation), then the sold VALUE TOKENS detach and travel in a STAGGERED,
ARCING STREAM (~70ms apart) down to the OPERATOR CR readout — each rising with a soft growing
shadow, then landing with a gentle settle and a "+N CR" that eases up and fades. The CR number
counts up smoothly and gives ONE gentle scale "punch" as tokens arrive. The sold cards ease out
with a soft dissolve, and the good's price pile visibly settles down to reveal its new lower top
token. Show a few hand cards + the value-token pile + a CR readout + a SELL trigger.
```
